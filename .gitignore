Thumbs.db
.DS_Store
*.log
node_modules/
.vscode/
.idea/
*.tmp
*.bak
.verify/

# Dev tooling / skills — not part of the website product
impeccable-main/
.agents/
.claude/
.codex/

# Source video master (the compressed images/hero/hero.mp4 is what ships)
video-hero/

# Source assets the user sends for processing (compressed copies live in images/)
USER GỬI CHO AI/

# Original standalone map app — already vendored into /map/ (has its own .git)
CAO BANG SUPER MAP/
